@model IEnumerable<Shop2_videopoint.Models.Review>

<ul>
    @foreach (var item in Model)
    {
            <li>
                <h4>@item.Rating / 10</h4>
                @Html.DisplayFor(modelItem => item.Comment)
            </li>
    }
</ul>

